{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the JARVIS desktop shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
